@model SmartMealsWeb.ViewModel.MealFromViewModel
@{
    ViewBag.Title = "MealForm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Add Meal</h2>

@using (Html.BeginForm("Save", "Meals"))
{
    @Html.ValidationSummary(true, "Please fix the following errors.")

    <div class="form-group">
        @Html.LabelFor(m => m.Name)
        @Html.TextBoxFor(m => m.Name, new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.Name)

    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.DatePosted)
        @Html.TextBoxFor(m => m.DatePosted, "{0:d MMM yyyy }", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.DatePosted)

    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.MealTypeId)
        @Html.DropDownListFor(m => m.MealTypeId, new SelectList(Model.MealTypes, "Id", "Name"), "Select Meal Type", new { @class = "form-control" })
        @Html.ValidationMessageFor(m => m.MealTypeId)

    </div>
    <div class="form-group">
        @Html.LabelFor(m => m.Description)
        @Html.TextAreaFor(m => m.Description, 10, 15, null)
        @Html.ValidationMessageFor(m => m.Name)

    </div>


    @Html.HiddenFor(m => m.Id)
    @Html.AntiForgeryToken()
    <button type="submit" class="btn btn-primary">Save</button>

}

@section scripts
    {
    @Scripts.Render("~/bundles/jqueryval")
}